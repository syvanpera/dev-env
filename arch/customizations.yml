---
- include_vars: ../vars.yml
- include_vars: vars.yml
- name: Apply custom Gnome settings
  become: yes
  become_user: "{{ username }}"
  block:
    - dconf:
        key: /org/gnome/gnome-flashback/desktop-background
        value: "true"
        state: present
