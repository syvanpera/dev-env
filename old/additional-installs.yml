- name: Ranger dev-icons
  become: yes
  become_user: "{{ username }}"
  git:
    repo: 'https://github.com/alexanderjeurissen/ranger_devicons'
    dest: '{{ home }}/.config/ranger/plugins/ranger_devicons'
    depth: 1
