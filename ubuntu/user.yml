---
- include_vars: ../vars.yml
- user:
    name: "{{ username }}"
    state: present
  register: user_registered
- name: Create home folders
  file:
    path: "{{ user_registered.home }}/{{ item }}"
    state: directory
  loop: "{{ folders.home }}"
