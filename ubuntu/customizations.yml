---
- include_vars: ../vars.yml
- include_vars: vars.yml
- name: Set up Gnome keybindings
  become: yes
  become_user: "{{ username }}"
  dconf:
    key: "/org/gnome/desktop/wm/keybindings/{{ item.key }}"
    value: "{{ item.value | string }}"
    state: present
  with_dict: "{{ keybindings }}"
